import React from 'react'

function MAY15() {
    // HTML
    // diff div and span - inline and block
    // sematic tags - will helps in the search engine optimization also
    // img is inline elements
    // inline elements height and width wont work
    //  - replace inline elemts , non replace inline elements

    // CSS - 
    // Casading is in which order the css is applied 

    // specificity - priority 
    // default position - static
    // flexbox - is an layout system 

    // box model - content , padding , margin  , border
    // content box , border box  ------------------------------------------------------------

    // WHY DO WE JS ?
    // make user to iteraactive

    // DIFF PARAMENTER AND ARGUMENTS ? 
    // def greet(name, greeting="Hello"):  # name and greeting are parameters
    // print(f"{greeting}, {name}!")
    // greet("Alice")  # "Alice" is the argument for the name parameter
    // greet("Bob", "Hi")  # "Bob" and "Hi" are arguments for name and greeting respectively
    
    // to replace CALLBECK HELL , promise is built

    // DECLARATIVE PROGRAMMIGN- 100 in js 50 in react js 
    // we only need to update the state
    // IMPERATIVE PROGRAMMING- we need to create a state and we need to update the dom also
    // example creating state functionalities (normal js) and js functionalities (getdocumentbyid, getdocumentbytagname, queryselectorall) 
    //  example :  normal js

    // TEMPORAL DEAD ZONE - error will occur , variable will be declared before it is intiallized

    // MICROTASKQUE - promise   call back que - settimeout , all the browser api 
    // CALLBACK QUE has less priority and microtask que has less priority

    // all the references will be treated as [1,2]  = [1,2] // false

  return (
    <div>MAY15</div>
  )
}

export default MAY15